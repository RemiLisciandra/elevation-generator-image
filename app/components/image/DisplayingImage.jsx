"use client";

export const DisplayingImage = ({ children }) => {
  return (
    <section className="flex-1 flex justify-center flex-col items-center gap-5">
      {children}
    </section>
  );
};

export default DisplayingImage;
